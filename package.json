{
  "name": "seattlesocial",
  "version": "1.0.0",
  "description": "Local events discovery platform for Seattle",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,client -c cyan,magenta \"npm run dev --prefix api\" \"npm run dev --prefix client\"",
    "start": "npm start --prefix api",
    "install:all": "npm install --prefix api && npm install --prefix client",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
